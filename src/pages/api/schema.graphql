"""
Описание запроса для документации

Есть **Markdown**
"""
type Query {
  "Привет из кода в документацию"
  hello: String
  blocks: [Block]
  block(id: ID!): Block
}

type Block {
  id: ID!
  content: String!
  from: [Block]
  to: [Block]
}